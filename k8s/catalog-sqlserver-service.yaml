apiVersion: v1
kind: Service
metadata:
  name: catalog-sqlserver
  namespace: fcg
spec:
  selector:
    app: catalog-sqlserver
  ports:
    - name: sqlserver
      protocol: TCP
      port: 1433
      targetPort: 1433
  type: ClusterIP