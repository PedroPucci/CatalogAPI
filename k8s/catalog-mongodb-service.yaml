apiVersion: v1
kind: Service
metadata:
  name: catalog-mongodb
  namespace: fcg
spec:
  selector:
    app: catalog-mongodb
  ports:
    - name: mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP